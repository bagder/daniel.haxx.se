#include "doctype.t"
#include "body.t"
#include "setup.t"

HEAD(roffit: converts man pages to HTML)
#include "daniel.t"

TITLE(roffit)
BOXTOP
<p>
 roffit converts input nroff man pages to output HTML pages.
<p>
 Example conversions (possibly not generated with the latest version):
<ul>
<li> the '<a href="https://curl.se/docs/manpage.html">curl</a>' man page
<li> the '<a href="https://curl.se/libcurl/c/curl_easy_setopt.html">curl_easy_setopt</a>' man page
</ul>


SUBTITLE(Download roffit)
<p>
 Get roffit directly from git, we don't have any proper download archives.

SUBTITLE(Bug Reports)
<p>

 We use the <a href="https://github.com/bagder/roffit/issues">github issue
tracker</a>

SUBTITLE(source code)
<p>
 <a href="https://github.com/bagder/roffit">Browse git</a>
<p>
 Checkout from git:
<pre>
 $ git clone git://github.com/bagder/roffit.git
</pre>
<p>
 See further on <a href="https://github.com/bagder/roffit">github</a>.

SUBTITLE(Changelog)

<p>
 We do releases very seldom. Check the <a href="https://github.com/bagder/roffit/commits/master">commitlog</a> for the most recent changes.

BOXBOT

#include "footer.t"

